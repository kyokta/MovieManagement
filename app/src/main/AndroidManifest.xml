<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movies"
        tools:targetApi="31">
        <activity
            android:name=".Home.DetailsMember"
            android:exported="false" />
        <activity
            android:name=".Admin.Home.DetailsAdmin"
            android:exported="false" />
        <activity
            android:name=".DetailsMovie"
            android:exported="false" />
        <activity
            android:name=".Admin.Home.AddMovie"
            android:exported="false" />
        <activity
            android:name=".Admin.MainAdmin"
            android:exported="false" />
        <activity
            android:name=".LoginRegister.LoginRegister"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>